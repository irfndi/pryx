name: Publish TypeScript SDK

on:
  workflow_dispatch:
    inputs:
      pryx_release_tag:
        description: Released pryx tag whose binaries should be bundled
        required: true

permissions:
  contents: read

jobs:
  publish:
    name: Publish @1jehuang/pryx-sdk
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: sdk/typescript
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: npm
          cache-dependency-path: sdk/typescript/package-lock.json

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Enforce Rust and TypeScript SDK surface parity
        working-directory: ${{ github.workspace }}
        run: cargo test -p pryx-sdk parity -- --nocapture

      - name: Install dependencies
        run: npm ci

      - name: Validate SDK
        run: npm run check

      - name: Download released pryx runtimes
        working-directory: ${{ github.workspace }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p sdk-runtime-assets
          gh release download "${{ inputs.pryx_release_tag }}" --dir sdk-runtime-assets \
            --pattern 'pryx-linux-x86_64.tar.gz' \
            --pattern 'pryx-linux-aarch64.tar.gz' \
            --pattern 'pryx-macos-x86_64.tar.gz' \
            --pattern 'pryx-macos-aarch64.tar.gz' \
            --pattern 'pryx-windows-x86_64.tar.gz' \
            --pattern 'pryx-windows-aarch64.tar.gz'

      - name: Prepare platform packages
        working-directory: ${{ github.workspace }}
        run: bash scripts/prepare_sdk_runtime_packages.sh sdk-runtime-assets

      # These must exist before the main package is published. npm selects only
      # the package matching the consumer's os/cpu from optionalDependencies.
      - name: Publish platform runtime packages
        working-directory: ${{ github.workspace }}
        run: |
          for package in sdk/npm/*; do
            npm publish "$package" --access public --provenance
          done

      - name: Publish with npm provenance
        run: npm publish --access public --provenance
