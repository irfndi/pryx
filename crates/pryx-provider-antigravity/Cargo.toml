[package]
name = "pryx-provider-antigravity"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
pryx-provider-gemini = { path = "../pryx-provider-gemini" }
pryx-schema-dialect = { path = "../pryx-schema-dialect" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
