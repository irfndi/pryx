[package]
name = "pryx-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
pryx-message-types = { path = "../pryx-message-types" }
serde = { version = "1", features = ["derive"] }
