[package]
name = "pryx-provider-copilot"
version = "0.1.0"
edition = "2024"

[dependencies]
pryx-message-types = { path = "../pryx-message-types" }
pryx-schema-dialect = { path = "../pryx-schema-dialect" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
