[package]
name = "pryx-sdk"
version = "0.1.0"
edition = "2024"
publish = false
description = "Rust SDK for the pryx harness API: connect, drive sessions, stream events"

[dependencies]
pryx-harness-api = { path = "../pryx-harness-api" }
jsonschema = { version = "0.49.3", default-features = false }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"

[dev-dependencies]
